<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- This Info.plist is authored by hand (GENERATE_INFOPLIST_FILE = NO) so we
	     own the Bundle* keys instead of letting the generator derive them.
	     PRODUCT_NAME remains lowercase so Xcode and the CLI agree on the
	     executable name. TERMINAL_DISPLAY_NAME supplies Terminal for Release and Terminal
	     Debug for Debug. The release bundle id stays com.github.tigercosmos.terminal — it's the app's
	     identity for Sparkle and code signing, so it must not change. Dynamic
	     values use build-setting substitution to stay in sync. -->

	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>

	<!-- User-visible name. CFBundleName drives the app menu (bold menu next to
	     the Apple menu); CFBundleDisplayName is what Finder/Dock show. -->
	<key>CFBundleName</key>
	<string>$(TERMINAL_DISPLAY_NAME)</string>
	<key>CFBundleDisplayName</key>
	<string>$(TERMINAL_DISPLAY_NAME)</string>

	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>

	<!-- Finder exposes this app service in a folder's context menu. AppKit
	     forwards the selected paths to TerminalApplicationDelegate.openInTerminal. -->
	<key>NSServices</key>
	<array>
		<dict>
			<key>NSMenuItem</key>
			<dict>
				<key>default</key>
				<string>New $(TERMINAL_DISPLAY_NAME) Project Here</string>
			</dict>
			<key>NSMessage</key>
			<string>openInTerminal</string>
			<key>NSRequiredContext</key>
			<dict>
				<key>NSTextContent</key>
				<string>FilePath</string>
			</dict>
			<key>NSSendTypes</key>
			<array>
				<string>NSFilenamesPboardType</string>
				<string>public.plain-text</string>
			</array>
		</dict>
	</array>

	<!-- Programs running inside the terminal (e.g. Claude Code voice dictation)
	     request mic access attributed to Terminal as the responsible process. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>A program running within Terminal would like to use your microphone.</string>

	<!-- Sparkle auto-update configuration. Both values are deliberately empty:
	     this fork has no release host and no signing key of its own yet, and a
	     feed URL on a domain we do not control must never ship. The in-app
	     updater stays disabled until both are set — see RELEASING.md. -->
	<key>SUFeedURL</key>
	<string></string>
	<key>SUPublicEDKey</key>
	<string></string>
</dict>
</plist>
